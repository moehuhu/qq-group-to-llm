/**
 * 提示词模板的默认值。
 *
 * 与 Schema 定义分开存放，免得几百行模板把配置结构淹没。
 * 用户可在插件配置里改写；占位符由 llm/prompt.ts 的 fill() 替换。
 */

export const TOPIC =
  `你是群聊记录总结助手。请阅读下面的群聊记录，提取最多 {maxTopics} 个主要话题。

对每个话题请给出：
1. 话题名称：简明扼要，直接点出主题
2. 主要参与者：最多 5 人，使用昵称
3. 话题描述：讲清楚起因、经过、结论。写具体内容而不是"讨论了某某问题"这类空泛描述；描述中使用昵称而非用户 ID；使用纯文本，不要 markdown 语法

群聊：{groupName}
时间范围：{timeRange}
用户额外关注：{query}

群聊记录：
{messages}

请严格按以下 YAML 格式返回，并放在 markdown 代码块中：
\`\`\`yaml
- topic: "话题名称"
  contributors:
    - "昵称1"
    - "昵称2"
  detail: |-
    话题描述，可多行
\`\`\``

export const GOLDEN_QUOTES =
  `请从下面的群聊记录中挑出最多 {maxGoldenQuotes} 条最有意思的「金句」。

挑选标准：观点新颖、表达生动、或有反差感与记忆点的原创发言。跳过纯粹的网络热词堆砌和复读。
金句是单句成立的发言——需要上下文才能看懂的片段不要选，那部分由「高光对话」负责。

群聊：{groupName}
时间范围：{timeRange}

群聊记录：
{messages}

请严格按以下 YAML 格式返回，并放在 markdown 代码块中：
\`\`\`yaml
- content: |-
    金句原文
  sender: "发言人昵称"
  reason: |-
    入选理由，纯文本
\`\`\`

如果没有值得收录的金句，返回空列表：
\`\`\`yaml
[]
\`\`\``

export const HIGHLIGHT_DIALOGUES =
  `请从下面的群聊记录中截取最多 {maxHighlightDialogues} 段「高光对话」。

「高光对话」指同时满足以下两条的连续多轮发言，缺一不可：

1. 有学术要素：用到某个学科的术语、定理、公式、模型、论文、实验方法或研究范式——不限于理工科，
   哲学、语言学、经济学、法学、历史学同样算。哪怕只是拿这些东西来讨论鸡毛蒜皮的日常也成立。
2. 是冷幽默：笑点来自一本正经的推演、精确到荒谬的措辞、把小事严肃化或把大事平淡化的反差、
   以及绷着脸的一本正经。靠夸张语气、感叹号、颜文字、复读或网络热梗堆出来的搞笑不算。

截取要求：
- 按原始时间顺序连续截取，不要把相隔很远的发言拼接成一段
- 原文照抄，不要改写、润色、补全或合并多条发言；发言以「[引用 昵称] 原话」开头的，
  表示它回复的是那句话，照抄时连这一行一起保留
- 同一段对话只收录一次，不同段落之间不要内容重复
- 只收多轮对话：单句就能看懂的精彩发言不归这里管，由「金句」另行收录
- 宁缺毋滥：没有同时满足上面两条的片段，就返回空列表，不要降低标准硬凑

群聊：{groupName}
时间范围：{timeRange}

群聊记录：
{messages}

请严格按以下 YAML 格式返回，并放在 markdown 代码块中：
\`\`\`yaml
- title: "一句话概括这段对话在聊什么"
  lines:
    - sender: "昵称"
      content: |-
        原文照抄
    - sender: "昵称"
      content: |-
        原文照抄
  reason: |-
    笑点在哪，纯文本一句话
\`\`\`

格式上有两处容易写错，请特别注意：
- 每段对话以 \`- title:\` 开头，不要用 \`1.\` \`2.\` 这样的编号
- content 是块标量：原话有几行就照抄几行，但每一行都必须缩进到和第一行完全相同的列

少缩进一格、或者把 \`-\` 写成编号，整段 YAML 都会解析失败。

如果没有任何符合条件的对话，返回空列表：
\`\`\`yaml
[]
\`\`\``

export const QUERY =
  `你是群聊记录问答助手。请只依据下面的群聊记录回答用户的问题。

规则：
- 记录里没有的信息，直接说明"记录中没有相关内容"，不要编造
- 回答中使用昵称而非用户 ID
- 用纯文本回答，不要使用 markdown 语法，控制在 300 字以内

群聊：{groupName}
当前时间：{currentTime}
记录时间范围：{timeRange}

群聊记录：
{messages}

用户问题：{query}`

export const USER_PERSONA =
  `你是一名社群观察员。请基于该用户的聊天记录，给出一份中性、克制的用户画像。

步骤：
1. 通读「聊天记录」，这是该用户最近 {lookbackDays} 天的发言
2. 仅依据这批记录归纳结论，不要假设该用户在此之外还有别的特征

要求：
- 只写记录能支撑的结论，不要推测用户的真实身份、职业、住址等隐私信息
- 保持中性描述，不做褒贬评价
- 纯文本，不要 markdown 语法
- evidence 只填记录中 <msgid:xxx> 里的 id 原文，挑 5-10 条最有代表性的，不要编造 id

聊天记录：
{messages}

请严格按以下 YAML 格式返回，并放在 markdown 代码块中：
\`\`\`yaml
- userId: "{userId}"
  username: "{username}"
  summary: |-
    整体印象，200 字以内
  keyTraits:
    - "性格特质"
  interests:
    - "关注的主题或爱好"
  communicationStyle: |-
    表达风格与情绪倾向，100 字以内
  evidence:
    - "msgid"
\`\`\``
